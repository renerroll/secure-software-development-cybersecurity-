Лабораторна робота
Практична криптографія під час розробки. Аналіз і покращення конфігурації криптографiї серверної частини

У сучасній веброзробці все бiльше вiдповiдальностi за гарантування безпеки лягає на розробника. Один iз критичних аспектiв — це правильне використання криптографiї для захисту облiкових даних, авторизацiї, шифрування та цiлiсностi даних. У цiй лабораторнiй роботi ви отримаєте можливiсть проаналiзувати приклади конфiгурацiй серверiв, що реалiзують JWT, шифрування та зберiгання паролiв, i виявити у них помилки.

Це змоделює реальну задачу перевiрки чужого коду або конфiгурацiї на безпечнiсть (code review / security audit) і дасть можливість спробувати самостійно на практиці створити код, який буде реалізовувати механізми криптографії.



Мета роботи

Ознайомитися з типовими елементами криптографiчної конфiгурацiї серверiв;
Навчитись виявляти помилки, пов'язанi з використанням застарiлих або небезпечних алгоритмiв;
Сформувати уявлення про те, якою має бути сучасна безпечна криптоконфiгурацiя.


🛡️Ця лабораторна робота має продовження в домашньому завданні, де ви зможете застосувати здобуті знання на новому прикладі проєктування криптозахисту з нуля.


Вхідні дані

Вам надаються три текстових конфiгурацiйних файли у форматi YAML (з iменами crypto_config_we.yaml, crypto_config_le.yaml, crypto_config_mi.yaml). Кожен з них мiстить типову структуру налаштувань для:

JWT токенiв;
Безпечного зберiгання паролiв;
Шифрування даних.


Хід роботи

Частина 1. Аналіз помилок

1. Вiдкрийте кожен iз трьох YAML-файлiв.

2. Визначте мінімум 3 помилки або небезпечні практики в кожному конфiгурацiйному файлі.

3. Поясніть, чому це помилка i як її виправити.



Частина 2. Створення безпечної конфігурації

1. Створіть власний YAML / як варiант JSON-файл, що міститиме налаштування для jwt, шифрування та паролiв.

2. У кожному блоцi використайте сучаснi, рекомендованi алгоритми та практики.

3. Додайте коментарі до кожного пункту, чому вибрано це рiшення.



Форма звіту

Таблиця 1: Помилки в конфiгурацiях

Файл	Помилка	Чому це помилка	Як виправити


Таблиця 2: Власна конфігурація

Вставте свій YAML / JSON файли із коментарями.



🛡️Продовження цієї лабораторної роботи — у домашньому завданні. У ньому ви спроєктуєте криптозахист для власного мікросервісу, використовуючи здобуті знання з аналізу конфігурацій, вибору алгоритмів і принципів безпечної криптографії.