# Архітектурне ревʼю — report.md

## Короткий висновок
Шаблон містить архітектурні дефекти, які ускладнюють валідацію, реагування та автоматизовані перевірки: відсутні сигнал(и) OpenAPI/типізація, CI-сканування і контроль за секретами; мережеві правила надто відкриті. Нижче — щонайменше 5 важливих проблем із поясненнями та рекомендаціями.

---

### 🔴 Проблема №1 — Відсутня типізація / response_model / валідація
**Файл:** `main.py`
**Опис:** Ендпоїнти не використовують Pydantic-моделі та `response_model`. Дані не валідуються, OpenAPI сигнал слабкий.
**Порушений принцип:** Відсутність сигналу (OpenAPI), PoLP для контрактів.
**Пропозиція:** Додати Pydantic-моделі, типи параметрів та `response_model`; додати централізоване логування та метрики.

---

### 🔴 Проблема №2 — Хардкод/ненадійна автентифікація
**Файл:** `main.py`
**Опис:** Пароль перевіряється у коді; небезпечне парсення `request.json()`.
**Порушений принцип:** Конфіденційність, PoLP, відсутній сигнал аутентифікації.
**Пропозиція:** Використати Pydantic для тіла запиту, зберігати секрети поза кодом (Vault/SecretManager), застосувати JWT / зовнішній Identity-провайдер.

---

### 🔴 Проблема №3 — Невідповідність портів та відсутня `readinessProbe`
**Файли:** `Dockerfile` ↔ `deployment.yaml`
**Опис:** Порт контейнера неузгоджений (8000 vs 8080), що ламає probes і маршрутизацію.
**Порушений принцип:** Надійність і сигнал здоров'я.
**Пропозиція:** Вирівняти порти, додати `readinessProbe` і `livenessProbe`, використовувати імедж з digest або строгим тегом.

---

### 🔴 Проблема №4 — Секрети підвантажуються неконтрольовано (`envFrom`)
**Файл:** `deployment.yaml`
**Опис:** `envFrom: secretRef` підвантажує всі ключі; втрата PoLP і аудиту доступу.
**Порушений принцип:** PoLP, відсутність сигналу про використання секретів.
**Пропозиція:** Використати `valueFrom.secretKeyRef` для конкретних змінних або CSI/Vault; обмежити доступ через RBAC.

---

### 🔴 Проблема №5 — CI не виконує SCA / SAST / secret-scan
**Файл:** `ci.yml`
**Опис:** CI лише збирає образ — відсутні Trivy/Syft/Semgrep/Gitleaks/SBOM.
**Порушений принцип:** Відсутній сигнал постачання/цілісності артефактів.
**Пропозиція:** Додати окремі job-и для SBOM (syft), Trivy (image scan), Semgrep (SAST) і Gitleaks (secrets).

---

### 🔴 Проблема №6 — Відкритий Security Group (0.0.0.0/0)
**Файл:** `main.tf`
**Опис:** SG дозволяє трафік звідусіль — порушено мінімальні привілеї.
**Порушений принцип:** PoLP, мережеве сегментування.
**Пропозиція:** Обмежити CIDR, використовувати змінну `allowed_cidr` або дозволяти тільки внутрішній балансувальник.

---

## ✅ Чеклист сигналів (поточний стан)
| Сигнал / Контроль               | Є / Немає |
|---------------------------------|-----------|
| OpenAPI / `response_model`      | ❌        |
| Типізація даних (Pydantic)      | ❌        |
| Secrets через Vault / CSI       | ❌        |
| CI перевірки (Trivy, Semgrep)   | ❌        |
| Логування                       | ❌        |
| Image pinning / SBOM            | ❌        |
| Мінімальні привілеї (PoLP)      | ❌        |

---

## Пріоритетні кроки (рекомендація)
1. Вирівняти порти + додати readinessProbe (критично для доступності).
2. Додати Pydantic + `response_model` → автогенерація OpenAPI (сигнал).
3. Використати конкретні `secretKeyRef` або CSI/Vault.
4. Додати CI-сканування (Trivy/Syft/Semgrep/Gitleaks).
5. Обмежити Security Group (use `allowed_cidr` variable).

---
## Короткий тест (quiz)

1. **Який файл найкраще служить сигналом для DAST-сканера?**

   **Відповідь:** `yaml` (OpenAPI) — DAST використовує OpenAPI/schema для генерації запитів і fuzzing.

2. **Policy-as-Code дає змогу перевіряти будь-який YAML-файл, зокрема Helm-шаблони?**

   **Відповідь:** **Так** — OPA/Conftest можуть парсити й перевіряти YAML-структури.

3. **Приклад архітектурного сигналу для supply-chain перевірок?**

   **Відповідь:** SBOM (JSON / SPDX) — сигнал для інструментів SCA.

4. **Що порушує принцип мінімальних привілеїв (PoLP)?**

   **Відповідь:** IAM-role з дозволом `*:*` — надто широкі права.

5. **Приклад правильного застосування CSI + Vault?**

   **Відповідь:** `volumeMount` (наприклад `/mnt/secrets-store`) — секрети монтуються як файли.

6. **Що описує Secure-by-Design?**

   **Відповідь:** Система побудована так, що в ній усе має пройти контроль автоматично (перевірки вбудовані в архітектуру).

7. **Якщо в Helm `deployment.yaml` є `value: "sk_live_ABC123"`, то Vault CSI правильно використано?**

   **Відповідь:** **Ні** — plaintext у шаблоні — антипатерн.
Файли виправлень знаходяться в папці `fix/`.
