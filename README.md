# Secure Architecture Lab — виконані кроки та підсумок аналізу

## Коротко
Ця гілка містить виконане архітектурне ревʼю шаблону: аналіз файлів проєкту, `report.md` з розбором проблем і пропозиціями, а також приклади виправлень у папці `fix/`.

---

## Що проаналізовано ✅
- `main.py` — перевірка типізації, логіки аутентифікації, форматів відповіді
- `deployment.yaml` — секрети, probes, securityContext
- `main.tf` — мережеві правила (Security Group)
- `ci.yml` — наявність SCA / SAST / secret-scan
- `Dockerfile` — користувач, порт, pinned deps
- `task.md` — перевірено відповідність виконаних кроків завданню

---

## Короткий перелік виявлених проблем (мінімум 5)
1. Відсутня типізація / `response_model` / OpenAPI — `main.py`
2. Хардкод/ненадійна автентифікація та unsafe parsing — `main.py`
3. Невідповідність портів і відсутня `readinessProbe` — `Dockerfile` ↔ `deployment.yaml`
4. Секрети підвантажуються неконтрольовано (`envFrom`) — `deployment.yaml`
5. CI не містить SCA / SAST / secret-scan (Trivy/Syft/Semgrep/Gitleaks) — `ci.yml`
6. Security Group відкритий на `0.0.0.0/0` — `main.tf`

---

## Артефакти, які створено/оновлено 🔧
- `report.md` — повний звіт з кожною проблемою та рекомендаціями
- Папка `fix/` із прикладами змін:
  - `fix/main.py` (Pydantic, response_model, логування)
  - `fix/Dockerfile` (non-root, порт 8080, pinned deps)
  - `fix/deployment.yaml` (secretKeyRef, probes, securityContext)
  - `fix/main.tf` (змінна `allowed_cidr` замість `0.0.0.0/0`)
  - `fix/ci.yml` (приклади job-ів для SBOM/Trivy/Semgrep/Gitleaks)
  - `fix/requirements.txt`

---

## Відповідність пунктам `task.md` ✅/⚠️
- Підготовка середовища — ✅ (файли переглянуті локально)
- Частина 1 (аудит) — ✅ (аналіз `main.py`, `deployment.yaml`, `main.tf`, `ci.yml`, `Dockerfile`)
- Частина 2 (звіт) — ✅ (`report.md` створено у корені)
- Частина 3 (fix/) — ✅ (приклади виправлень у `fix/`)
- Частина 4 (передача) — ⚠️ (потрібно: ZIP для LMS або PR — оберіть опцію)

---

## Як перевірити локально (коротко) ▶️
- Зібрати образ: `docker build -t user-api .`
- Запустити контейнер: `docker run -e AUTH_SECRET=secret -p 8080:8080 user-api`
- Протестувати ендпоїнти: `curl "http://localhost:8080/user?id=1"`

Якщо встановлені інструменти:
- SBOM: `syft user-api:latest -o json`
- Image scan: `trivy image user-api:latest`
- SAST: `semgrep --config auto .`

---

## Рекомендовані наступні кроки
1. Закомітити зміни в окрему гілку і відкрити PR (рекомендовано).  
2. Інтегрувати Trivy/Syft/Semgrep/Gitleaks в CI.  
3. Міграція секретів до Vault/CSI + використання `secretKeyRef`.  
4. Обмежити `allowed_cidr` у `main.tf` і додати network ACLs.

---

## Що зроблю далі — вибір за тобою
- Зробити commit → створити гілку та PR; або
- Підготувати ZIP архів для LMS; або
- Просто залишити файли для ручного коміту.

> Напиши: `PR` — щоб я створив гілку + PR, `ZIP` — щоб я згенерував архів, або `Нічого` — якщо сам керуватимеш репо.
